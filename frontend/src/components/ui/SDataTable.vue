<script setup lang="ts" generic="T extends Record<string, unknown>">
defineProps<{ rows: T[]; columns: Array<{ key: keyof T; label: string }> }>();
</script>
<template><div class="overflow-x-auto rounded-lg border border-hairline bg-surface"><table class="w-full text-sm"><thead class="bg-surface-sunken text-left text-xs uppercase tracking-wide text-ink-500"><tr><th v-for="column in columns" :key="String(column.key)" class="px-4 py-3">{{ column.label }}</th><th v-if="$slots.actions" class="px-4 py-3" /></tr></thead><tbody class="divide-y divide-hairline"><tr v-for="(row, index) in rows" :key="index"><td v-for="column in columns" :key="String(column.key)" class="px-4 py-3 text-ink-700"><slot :name="`cell-${String(column.key)}`" :row="row">{{ row[column.key] }}</slot></td><td v-if="$slots.actions" class="px-4 py-3 text-right"><slot name="actions" :row="row" /></td></tr><tr v-if="!rows.length"><td :colspan="columns.length + ($slots.actions ? 1 : 0)" class="px-4 py-8 text-center text-ink-500">No records found.</td></tr></tbody></table></div></template>
